# Copy this file to gcp-vm/.env.secrets and set real local-only secret values.
# This file is gitignored and should never be committed with real credentials.

# Optional DuckDNS token used by vm-ctl.sh
DUCKDNS_TOKEN=

# Steam Headless credentials propagated to the VM startup script
USER_PASSWORD=change-me
SUNSHINE_PASS=change-me
