# Copy to `.env.secrets` (local, ignored by git) and keep real values only there.

# Optional DuckDNS token for multi-instance deployments
DUCKDNS_TOKEN=

# Steam Headless credentials injected into Kubernetes secrets
USER_PASSWORD=change-me
SUNSHINE_PASS=change-me
